export type Water = {
    onHand: number
    capacity: number
    usageRate: number
}
export type Food = {
    onHand: number
    capacity: number
    usageRate: number
}
export type Medical = {
    onHand: number
    capacity: number
    usageRate: number
}