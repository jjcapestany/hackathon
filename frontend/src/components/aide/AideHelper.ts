export type Water = {
    onHand: number
    capacity: number
    useageRate: number
}
export type Food = {
    onHand: number
    capacity: number
    useageRate: number
}
export type Medical = {
    onHand: number
    capacity: number
    useageRate: number
}